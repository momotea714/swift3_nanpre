# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NumberPlace' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NumberPlace
pod 'Unbox'
pod 'KRProgressHUD'
pod 'SwiftR'
  target 'NumberPlaceTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NumberPlaceUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
